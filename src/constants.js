const localhost = "https://backend-hayat.herokuapp.com";

const apiURL = "/api";

export const endpoint = `${localhost}${apiURL}`;

export const loginUrl = `${localhost}/rest-auth/login/`
export const registerUrl = `${localhost}/rest-auth/registration/`